<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatisplus.xsbb531.mapper.Wl_531Mapper">

    <select id="selectAllwlname" resultType="java.lang.String">
        SELECT DISTINCT wl.name FROM wl_531 wl WHERE wl.name IS NOT NULL;
    </select>

</mapper>
